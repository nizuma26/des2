import { OptionsDatatable } from "./types";

export const DEFAULT_OPTIONS:OptionsDatatable = {
    checkbox: true,
    collapse: false,
    dense: true,
    pagination: true,
    search: true,
    key: 'id',
    toolbar: true,
    selectField: 'id'
}