import { BaseModalProps } from '../../../../../types';
import { Container } from '../../../../../types/configuration/containers';

export interface ContainerFormDialogProps extends BaseModalProps {
  values?: Container;
}
