export interface SkeletonConfigTypes {
  GLOBAL: {
    ANIMATION: 'wave' | 'pulse';
  };
  VERTICAL: {
    ITEM_HEIGHT: number | string;
  };
  MINI: {
    ITEM_HEIGHT: number | string;
  };
  HORIZONTAL: {
    ITEM_HEIGHT: number | string;
    ITEM_WIDTH: number | string;
  };
}
